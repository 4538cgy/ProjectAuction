<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="itemalarm"
            type="com.example.project_auction.data.AlarmDTO" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >
        <ImageView
            android:id="@+id/item_alarm_imageview"
            android:layout_width="50dp"
            android:layout_height="50dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_margin="24dp"
            />

        <TextView
            app:layout_constraintTop_toTopOf="@+id/item_alarm_imageview"
            app:layout_constraintBottom_toBottomOf="@+id/item_alarm_imageview"
            android:layout_marginLeft="12dp"
            android:id="@+id/item_alarm_textview_message"
            app:layout_constraintLeft_toRightOf="@+id/item_alarm_imageview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="message"
            />

        <TextView
            android:id="@+id/item_alarm_textview_timestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginRight="12dp"
            android:text="timestamp"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>